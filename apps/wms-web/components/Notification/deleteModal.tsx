import CommonContext from "@/contexts/CommonContext";
import { useContext } from "react";

export default function Notification() {
    const {modal} = useContext(CommonContext);
    // const deleteModal = ({title, content, onConfirm})
}